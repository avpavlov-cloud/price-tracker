version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: price_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: price_tracker
    ports:
      - "5432:5432"
